- array.map(() => { 중괄호일 경우에는 return이 들어가야 함})
- array.map(() => ( 괄호일 경우에는 return이 들어가지 않아도 됨))
- https://react-icons.netlify.com/#/icons/md 페이지에 들어가면 수많은 아이콘과 이름이 나타남
- 사용하고 싶은 아이콘을 고른 후 import 구문을 사용하려 불러와서 쓰면 됨
- e.preventDefault() : 기존에 갖고 있는 이벤트를 방지하기 위한 함수
- form의 onSubmit 이벤트를 사용하는 이유는 클릭 이벤트 뿐만 아니라 Enter도 반응하기 때문
  → 만약 버튼에서 onClick만 사용했다면 input에서 onKeyPress 이벤트를 통해 Enter를 감지하는 로직도 만들어야 함
- 배열의 지우기는 filter를 사용하여 특정 조건을 만족하는 원소들만 따로 추출해서 새로운 배열 만들기
- 배열의 수정은 map을 사용하여 배열을 순회하면서 if 문을 넣어 특정 조건일 경우에는 값이 바뀌도록 하기
